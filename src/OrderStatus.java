public enum OrderStatus {
        PENDING,        // Order has been placed but not started yet
        IN_PROGRESS,    // Order is being prepared
        READY,          // Order is ready to be served
        SERVED          // Order has been served
}
